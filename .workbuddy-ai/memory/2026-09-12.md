# 2026-09-12 — Perbaikan "CV AI tidak bisa translate beberapa kolom"

## Gejala
User melaporkan kolom JP di AI CV (ai_form) sebagian tidak ikut terisi saat
minta AI menerjemahkan.

## Akar masalah (3 bug, semua terverifikasi)

1. **State tidak tersinkron (regresi refactor a64ef65)** — `js/pages/ai-chat.ts`
   memakai `deps.latestCandidateData = merge(...)`. `deps` cuma objek sementara
   dari `getChatDeps()`, jadi variabel modul `latestCandidateData` di
   `js/pages/ai_form.ts` TIDAK ikut berubah → `updateFormUI()` & `saveToLocal()`
   membaca state lama → form tidak pernah terisi dari balasan AI (termasuk _jp).
   Sebelum refactor: `latestCandidateData = mergeCandidateData(...)` (benar).
   Fix: tambah `setLatestCandidateData` ke `ChatDeps` + dipanggil sebelum
   `updateFormUI()`.

2. **`setNested` tidak array-aware** (`netlify/functions/_lib/ai/chat.ts`) —
   selalu membuat `{}` untuk segmen numerik, jadi hasil translate
   pendidikan/pekerjaan/keluarga jadi `{"0": {...}}` (objek, bukan array) →
   frontend membacanya sebagai objek → baris riwayat kosong.
   Fix: buat `[]` bila segmen berikutnya numerik.

3. **Guard array `_id` asimetris** (`chat.ts`) — `else if (inAi && inAi !== origId)`
   tidak memulihkan `_id` bila model mengembalikannya sebagai string kosong
   (terbukti: `sekolah_id` jadi "", `perusahaan_id` hilang dari balasan).
   Fix: hapus syarat `inAi &&` supaya kontraknya sama dengan field non-array.

## Bonus fix (blocker build)
`npm run build` gagal sejak lama: `cv.ts` meng-import `buildMasterNested` dari
`actions-master.ts` yang tidak meng-ekspornya. Diperbaiki: export ditambahkan,
`@ts-expect-error` usang di cv.ts:175 dihapus. `npx tsc --noEmit` kini bersih.

## Verifikasi
- Probe nyata (Gemini asli): 21 & 30 field (teks panjang) semua terjemah OK;
  `sekolah_id`/`perusahaan_id` kini bertahan; array tetap array.
- Test baru: `netlify/functions/_lib/ai/chat.test.ts` (+2), `js/pages/ai-chat.test.ts` (baru, 2 test).
- `npx vitest run` → 391 pass / 3 fail (3 fail = `tests/ping.test.ts` yang usang,
  masih meng-import `{ handler }` padahal `ping.js` sudah v2 default export — pra-eksisting).
- `node scripts/build-js.mts` dijalankan → bundel `js/pages/ai_form.js` sudah memuat fix.

## Audit lanjutan: kolom Jiko PR & Wawancara (permintaan user)
Daftar kolom di `ai_form.html` bagian "4. Jiko PR & Wawancara (Auto-Translate)":
promosi, kelebihan, kekurangan, hobi, keahlian, motivasi, alasan_bidang,
rencana_pulang, keinginan, tujuan (+ riwayat_jepang dropdown, tanpa input JP).

Probe nyata dengan prompt persis "tolong translate kan semua kolom id ke jp",
semua `_id` terisi → **10/10 kolom JP terisi**. Tapi ditemukan:

1. **"Model-wins" bisa mengisi `_jp` dengan isi kolom lain.** Model balas JSON
   dan mengisi `motivasi_jp` dengan terjemahan milik `tujuan_ke_jepang`
   ("仕事の経験を積み…") padahal `motivasi_id` = "Membantu ekonomi keluarga…".
   Karena `covered` (nilai model dianggap final), batch terjemahan tidak
   menimpanya → kolom terisi tapi SALAH. 1 dari 4 `_jp` yang diisi model salah.
2. **`wawancara.riwayat_jepang_jp` tidak pernah diterjemahkan Gemini** — tidak
   ada di registry `jp-fields.ts`. Hanya diisi `autoPairFill()` (frontend) dan
   hanya jika nilainya persis BELUM PERNAH / PERNAH MAGANG / PERNAH TOKUTEI GINO.
   Nilai bebas → JP kosong.
3. **Guard `_id` tidak melindungi field di luar registry.** Model mengembalikan
   `riwayat_jepang: ""` dan guard tidak memulihkannya (di payload server hilang;
   frontend `mergeCandidateData` masih menyelamatkan karena string kosong
   diabaikan).
4. Kolom tanpa pasangan JP: `lama_di_jepang`, `harapan_gaji`, `harapan_tabungan`.

Usulan (belum dikerjakan, menunggu keputusan user): buat hasil batch terjemahan
menang atas `_jp` dari model pada giliran translate; dan/atau tambahkan
`riwayat_jepang` ke registry.

## Eksekusi fix A + B (disetujui user: "2 2 nya")

**Fix B — `riwayat_jepang` masuk registry.**
`jp-fields.ts` +`{ idPath:['wawancara','riwayat_jepang'], jpPath:['wawancara','riwayat_jepang_jp'] }`
(25 entri sekarang; tanpa formKey/jpCol karena hidup di `ai_data_json`).
`chat.ts` +`deterministicJpFor()` → nilai dropdown diisi kanji EKSAK dari
`RIWAYAT_JEPANG_PAIRS` tanpa panggilan Gemini; nilai di luar dropdown tetap
diterjemahkan Gemini lewat registry. `RIWAYAT_JEPANG_PAIRS` dipindah ke
`shared/silsilah.ts` (satu sumber dgn frontend `ai_form.ts`).

**Fix A — batch menang atas `_jp` model.**
`autoTranslateMissingJp(data, forcePaths, srcData)`: `forcePaths` = field yang
`_jp`-nya DIUBAH model di balasan ini → wajib diterjemahkan ulang. Urutan di
`handleProcessAIChat` wajib: hitung `forcePaths` (nilai model asli) → `snapAiDataToRegistry` →
batch. Kalau snap jalan lebih dulu, kanji registry terlihat seperti "model
mengubah _jp" dan justru dipaksa masuk batch.

**Bug tambahan yang ketemu saat verifikasi (fix C).**
Batch hanya membaca `_id` dari `p.currentData` → `_id` yang BARU diisi model
("hobi saya sepak bola") tidak pernah diterjemahkan di giliran yang sama,
`hobi_jp` tetap kosong. Fix: `srcData` (balasan model) dibaca sebagai GABUNGAN —
`_id`: currentData dulu, fallback srcData; `_jp`: currentData dulu, fallback
srcData (supaya kanji eksak snap tidak ditimpa terjemahan bebas). Loop array
pakai `Math.max(currentData.length, srcData.length)` → baris riwayat BARU dari
model ikut diterjemahkan.

**Guard `_id` (dari sesi sebelumnya, dikonfirmasi).**
`if (origId && inAi !== origId)` untuk non-array & array: field yang SUDAH
terisi dipulihkan byte-for-byte; field yang masih KOSONG menerima nilai model.

## Verifikasi akhir
- `npx tsc --noEmit` → 0 error.
- `npx vitest run` → **398 pass / 3 fail**. 3 fail = `tests/ping.test.ts` saja
  (usang, masih `import { handler }` padahal `ping.js` sudah v2 default export — pra-eksisting).
- `chat.test.ts` → 16 test hijau (2 assertion lama diubah ke kontrak "batch
  menang", +2 test baru: baris array baru diterjemahkan; kanji snap tidak
  ditimpa). `jp-fields.test.ts` → 24→25 entri + test khusus riwayat_jepang.
- `npm run build` → sukses penuh (tsc, check-globals, check-handlers, tailwind,
  i18n-jp, html, js). `js/pages/ai_form.js` ikut ter-rebuild.
- `npx esbuild netlify/functions/ai-chat.js ... --outfile=<relatif>` → BUNDLE_OK
  (verifikasi jalur bundling fungsi lolos).

## Temuan lingkungan (BUKAN dari perubahan ini)
1. `scripts/build-netlify-functions.sh` GAGAL TOTAL di Windows Git Bash: variabel
   `$ROOT` berisi path gaya MSYS (`/e/Asjpow4v7-main/khoci921`) lalu diteruskan ke
   `esbuild.exe` native Windows → `Could not resolve "/e/..."` → 20/20 fungsi gagal
   (termasuk yang tidak disentuh). Di Linux/Netlify normal. Solusi bila perlu
   jalankan lokal: pakai outfile RELATIF, bukan `$ROOT/...`.
   Efek samping: `--outfile="$ROOT/..."` membuat direktori stray `e/Asjpow4v7-main/...`
   di dalam repo (sudah dibersihkan).
2. `netlify/functions/_lib/session.test.ts` > "wrong secret produces different
   signature" FLAKY (~1 dari 3 run gagal). Test menandatangani dua token lalu
   membandingkan payload — payload memuat timestamp, jadi beda detik = beda hasil.
   Pra-eksisting, tidak berhubungan dengan AI translate. Belum diperbaiki.

## Catatan
- Belum di-commit & belum deploy (butuh izin pemilik).
- `docs/archive/DEBUG-TODO.md` tidak lagi aktif; root `DEBUG-TODO.md` tidak ada.
- `i18n`: tidak ada teks UI baru → tidak ada key baru.

---

# Lanjutan — bersihkan 3 masalah pra-eksisting (gate test jadi 100% hijau)

## 1. `tests/ping.test.ts` usang → diperbaiki
Tes masih memakai kontrak Netlify Functions **v1** (`import { handler }`,
`result.statusCode`, `result.body`, `result.headers`), padahal `ping.js` sudah
**v2** (`export default (req, context) => Response`). Ketiga tes selalu gagal.
Fix: `import handler from ...ping.js` + `new Request(url)` → `res.status`,
`await res.json()`, `res.headers.get(...)`. Tes "uptime increases between calls"
diganti jadi "uptime = detik non-negatif & tidak menurun" — uptime dihitung
`Math.floor(ms/1000)` jadi jeda 10 ms memang tidak bisa menaikkannya.

## 2. `netlify/functions/_lib/session.test.ts` FLAKY → diperbaiki
`it('wrong secret produces different signature')` menandatangani dua token lalu
`expect(verifyToken(t1)).toEqual(verifyToken(t2))`. `signToken` menyematkan
`iat`/`exp` dari `Date.now()` → beda milidetik = payload beda = gagal ~1/3 run.
Fix: `vi.useFakeTimers()` + `vi.setSystemTime(...)` (try/finally `useRealTimers`),
nama tes diganti sesuai maksud sebenarnya + assertion deterministik
(`token1 === token2`). Diverifikasi 5/5 run hijau.

## 3. `scripts/build-netlify-functions.sh` gagal total di Windows → diperbaiki
`$ROOT` (path gaya MSYS `/e/...`) diteruskan ke `esbuild.exe` native Windows →
`Could not resolve "/e/..."` → **0/20 fungsi** ter-bundle (termasuk yang tidak
diubah). Script ini LANGSUNG dipakai deploy: `netlify.toml`
`[build] command = "bash scripts/build-netlify-functions.sh"`.
Fix: `cd "$ROOT"` + semua path RELATIF (portabel Linux & Git Bash); stderr
esbuild DITANGKAP & dicetak saat gagal (dulu `2>/dev/null` → "❌ Failed" tanpa
sebab, itu sebabnya bug ini lama tak terlihat). Hasil: **20/20 fungsi** OK.
Catatan: `scripts/prebuild-netlify.sh` (strip `.ts` via `sed -i` ke file sumber)
TIDAK direferensikan siapa pun = dead code & berbahaya bila dijalankan. Belum dihapus.

## 4. (Bonus) `vitest.config.ts` menjalankan suite `_lib` DUA KALI
`exclude` di-override tanpa `.netlify`. Karena `build-netlify-functions.sh`
menyalin `netlify/functions/_lib` (termasuk 18 `*.test.ts`) ke
`.netlify/functions/_lib`, vitest ikut menjalankannya → 35 → 53 file test,
angka hasil menyesatkan. Fix: tambah `.netlify` + `dist` ke `exclude`.

## Hasil akhir gate
- `npx tsc --noEmit` → 0 error.
- `npx vitest run` → **35 file / 401 tes, SEMUA PASS** (sebelumnya 3 fail).
- `bash scripts/build-netlify-functions.sh` → 20/20 fungsi.
- `npm run build` → sukses penuh.
- Change set: 14 file dimodifikasi + 2 file baru (`js/pages/ai-chat.test.ts`,
  `.workbuddy-ai/memory/2026-09-12.md`). Belum commit/deploy (butuh izin pemilik).

---

# Lanjutan 2 — verifikasi API nyata + temuan gate lint

## ✅ Verifikasi END-TO-END dengan Gemini ASLI (bukti keluhan awal beres)
Probe sementara (`probe-real.test.ts`, memuat `.env.local` manual tanpa dotenv,
lalu `handleProcessAIChat({flow:'apply', history:[{content:'tolong translate kan
semua kolom id ke jp'}]})`): semua 11 kolom `_id` Jiko PR & Wawancara terisi →
**11/11 kolom JP terisi**.
- Batch = **1 panggilan Gemini** untuk 10 field (delimiter `###`).
- `riwayat_jepang_jp` = `技能実習経験` diisi **deterministik** dari registry
  (PERNAH MAGANG), TIDAK ikut batch → fix B terbukti.
- `motivasi_jp` = `家族の経済を助け、自己成長することです。` yang COCOK dengan
  `motivasi_id` "Membantu ekonomi keluarga dan mengembangkan diri." — bug
  "salah kolom" yang diperbaiki fix A tidak muncul lagi.
- Probe sudah dihapus (bukan bagian test default karena memanggil API berbayar).

## ⚠️ Temuan: gate `npm run lint` RUSAK TOTAL (butuh keputusan pemilik)
`eslint.config.js` meng-`import tseslint from 'typescript-eslint'`, TAPI:
- `typescript-eslint` **tidak ada di `package.json`** (25 dependensi lain lengkap
  ter-install — jadi ini dependensi tak-dideklarasikan, bukan drift install).
- `npm install -D typescript-eslint` GAGAL: `typescript-eslint@8.70.0` minta peer
  `typescript >=4.8.4 <6.1.0`, sedangkan repo memakai `typescript ^7.0.2`.
- Versi `typescript-eslint` TERBARU yang tersedia = 8.70.0 → **belum ada rilis
  yang mendukung TS 7**. Jadi tidak bisa dibereskan sekadar dengan install.
- Konsekuensi: `npm run lint` gagal di detik pertama, dan `scripts/ship.sh`
  berhenti di langkah 3/5 → alur "wajib commit & push" di README tidak bisa jalan.
- Belum diubah (sengaja). Opsi: turunkan `typescript` ke <6.1 (mengubah toolchain
  inti: tsc/esbuild/vitest), atau tunggu rilis typescript-eslint yang dukung TS7,
  atau lepas import `typescript-eslint` dari eslint.config.js (kehilangan lint .ts),
  atau `--legacy-peer-deps` (parser dianggap tidak kompatibel — berisiko).
  `package.json` & `package-lock.json` TIDAK berubah (install gagal bersih).

## Sisa item terbuka (menunggu keputusan pemilik)
1. `scripts/prebuild-netlify.sh` = dead code (tidak direferensikan di config mana
   pun) DAN berbahaya bila dijalankan: `sed -i` menulis ulang file sumber
   `netlify/functions/_lib` in-place untuk membuang ekstensi `.ts`. Belum dihapus.
2. Gate lint rusak (lihat di atas).
3. Belum commit & belum deploy.

---

# Lanjutan 3 — sinkronisasi dokumentasi + cek format

## Dokumentasi yang diperbarui (wajib menurut AGENTS.md)
Registry 24→25 + semantik `forcePaths`/`srcData` + urutan snap membuat dua dokumen
jadi usang:
- `docs/AI_CV_FULL_FLOW.md` — bagian **Auto-Translate** ditulis ulang: registry
  `jp-fields.ts` (25 nested + 6 array), dua kelas field yang ikut batch, "batch
  selalu menang", sumber nilai GABUNGAN currentData+balasan model, prefill
  deterministik `riwayat_jepang`, urutan wajib forcePaths→snap→batch, kontrak guard
  `_id`. Ditambah bagian **State sync** (kenapa `setLatestCandidateData` wajib —
  `deps.latestCandidateData = ...` tidak menyentuh modul).
- `docs/AI_DATA_FLOWS.md` — alur `processAIChat` dikoreksi (dulu tertulis
  `autoTranslateMissingJp(aiData)` lalu `autoTranslateMissingJp(p.currentData)`,
  padahal SATU panggilan `(currentData, forcePaths, aiData)`); klaim
  "AI_ID_JP_PAIRS 24 pairs" → satu registry `jp-fields.ts` + test pengunci;
  Conflict Matrix & checklist "Sebelum Update Fitur AI" diarahkan ke
  `JP_FIELD_PAIRS`/`shared/silsilah.ts`.

## Cek format (prettier) — hanya perbaiki yang memang salahku
`format:check` BUKAN bagian gate `ship.sh` (gate = build → test → lint), tapi tetap
dijaga agar tidak menambah churn. Dicek HEAD vs working:
- SUDAH kotor di HEAD (jangan disentuh — reformat = diff raksasa tak terkait):
  `chat.ts`, `jp-fields.ts`, `shared/silsilah.ts`, `js/pages/ai_form.ts`,
  `js/pages/ai-chat.ts`, `docs/AI_CV_FULL_FLOW.md`, `docs/AI_DATA_FLOWS.md`.
- HEAD bersih → dinormalkan: `tests/ping.test.ts` (1 baris) dan
  `netlify/functions/_lib/ai/chat.test.ts` (1 baris, `--write`).
- `vitest.config.ts` & `session.test.ts` sudah bersih sejak awal.
Cara cek cepat: `git show HEAD:<file> > tmp && npx prettier --check tmp`.

## Catatan inkonsistensi AGENTS.md
AGENTS.md mewajibkan "update DEBUG-TODO.md SEBELUM commit", tapi file itu hanya ada
di `docs/archive/DEBUG-TODO.md` (arsip, tidak aktif) — root `DEBUG-TODO.md` tidak
ada. Jadi instruksi itu tidak bisa dijalankan apa adanya. Perlu keputusan pemilik:
buat ulang DEBUG-TODO.md aktif, atau perbarui AGENTS.md.

## Status akhir (semua gate yang bisa dijalankan)
- `npx tsc --noEmit` → 0 error · `npx vitest run` → **35 file / 401 tes PASS**
- `bash scripts/build-netlify-functions.sh` → 20/20 · `npm run build` → sukses
- `npm run lint` → masih ❌ (konflik peer TS7, butuh keputusan pemilik)
- Change set: **16 file dimodifikasi + 2 file baru**. Belum commit & belum deploy.

---

# Lanjutan 4 — FIX "admin & siswa logout tiap selesai benerin CV AI" + audit fix translate

## Akar masalah (bug logout) — BUKAN sesi kedaluwarsa, tapi false-positive
`bukaMasterEksternalAdmin()` (js/03_candidate.ts) membuka AI CV lewat
`generateAiFormBridge` → `bukaFormBridge` → `window.location.href`, yaitu
**tab yang SAMA** (bukan window baru), sehingga ai_form.html **mewarisi
localStorage ADMIN**.

Rantai bug:
1. Guard VIP `verifikasiAksesAiCv()` di `js/pages/ai_form.ts` hanya
   early-return saat `asj_admin_login==='sukses'`; jalur kedua
   (`asj_kandidat_login!=='sukses'` → return true) tetap lolos lalu memanggil
   `callAPI('getAppData', ['kandidat', targetWa])`.
2. `api-client.ts` memilih token berdasarkan
   `CANDIDATE_ACTIONS`/`getAppData:'kandidat'`: karena `asj_admin_login==='sukses'`
   → yang terkirim **token ADMIN**, bukan token kandidat.
3. `handleGetAppData` (actions-public.ts): `valid = t && t.role===role &&
   ...` dengan `role='kandidat'` → token admin `role==='admin'` → **valid=false**
   → balas `sessionInvalid: true` padahal token admin masih SAH.
4. `api-client.ts` blok `sessionInvalid` → refresh gagal (role retry salah) →
   **menghapus SEMUA sesi** (`asj_admin_login/session`, `asj_kandidat_*`) +
   `window.location.reload()` → admin mendarat di layar login.

## Perbaikan
1. **Server (`netlify/functions/_lib/actions-public.ts` → handleGetAppData)** —
   tambah `isAdminView`: bila mode `kandidat` tapi token valid ber-role
   `admin` (dan `kind !== 'refresh'`), JANGAN anggap sesi mati. Tetap lanjut
   ambil data kandidat target dan **tidak menyertakan `sessionInvalid`**.
   - `w` (WA target) untuk mode kandidat: `t.wa` dulu; kalau token admin
     (tak punya wa) → fallback `payload[1]` (admin memang berwenang memilih).
   - Token refresh & token benar-benar rusak/kadaluwarsa TETAP `sessionInvalid`
     (isAdminView mensyaratkan `t` truthy).
   - Pola ini konsisten dengan `isOwnerOrAdmin()` dan `handleSubmitDataAsj`
     yang sudah menerima dua role.
2. **Frontend (`js/pages/ai_form.ts` → verifikasiAksesAiCv)** — guard tidak
   lagi memanggil `getAppData('kandidat')` kalau `asj_kandidat_session` tidak
   ada. Lapisan pertahanan kedua: walau server salah balas, guard tak akan
   memicu request ber-token admin yang bisa menghapus sesi.

## Verifikasi
- Test regresi baru di `netlify/functions/_lib/dispatch.test.ts` (+2):
  - `getAppData kandidat + token ADMIN → bukan sessionInvalid` — **terbukti
    GAGAL di kode lama**, lolos setelah fix (diverifikasi dgn `git stash`).
  - `getAppData kandidat + token REFRESH admin → tetap sessionInvalid`
    (guard negatif; token refresh tidak boleh lolos).
- `npx vitest run` → **35 file / 403 tes PASS** (naik dari 401).
- `npx tsc --noEmit` → 0 error. `npm run build` → sukses.
  `bash scripts/build-netlify-functions.sh` → **20/20**.
- Fix terverifikasi ikut ter-bundle: `isAdminView` ada di
  `.netlify/functions/{ai-chat,ai-form-submit,admin-ai-context}.js`, dan
  `js/pages/ai_form.js` memuat guard `asj_kandidat_session`.

## Audit fix translate sesi sebelumnya — hasil: SEMUA BERTAHAN
Diverifikasi ulang dari kode + test, tidak ada regresi:
- `setNested` array-aware (`[]` bila segmen berikutnya numerik) — benar.
  Diuji hazard "satu key dipakai array DAN objek": `pendidikan/pekerjaan/
  keluarga` **array-only** di kedua proyeksi registry, `kenalan_jepang`
  **objek-only** → tidak ada key campuran, hazard **tidak reachable**.
- Guard `_id` asimetris sudah simetris (hapus syarat `inAi &&`) — _id kosong
  dari model kini dipulihkan untuk field nested DAN array.
- `forcePaths` → `snap` → `batch` urutannya benar (kalau snap dulu, kanji
  registry akan terlihat seperti "model ubah _jp" dan dipaksa masuk batch).
- `deterministicJpFor` untuk `riwayat_jepang`: prefill kanji eksak, dan
  `forcePaths` me-skip `deterministic[key]` supaya tidak ditimpa.
- "Batch selalu menang" atas `_jp` dari model — berfungsi (test hijau).
- Baris array BARU (index ≥ panjang currentData) ikut diterjemahkan — benar.
- `riwayat_jepang` sengaja TANPA `formKey/jpCol` (kolom JP tak ada di master,
  hidup di ai_data_json) — konsisten.

## Status
- Change set kumulatif: **18 file dimodifikasi + 2 file baru**. Belum commit
  & belum deploy (sesuai AGENTS.md butuh izin pemilik).
- Masih terbuka: gate `npm run lint` rusak (typescript-eslint vs TS 7),
  `scripts/prebuild-netlify.sh` dead code, `DEBUG-TODO.md` tidak ada.

---

# Lanjutan 5 — Verifikasi "apakah semua update masuk dengan benar di build Rirekisho?"

Pertanyaan pemilik setelah update: apakah semua perubahan benar-benar sampai ke
build/deploy Rirekisho. Diverifikasi dari kode + simulasi alur data nyata.

## Hasil: PIPELINE BUILD/DEPLOY SEHAT — semua update ikut ter-bundle
- `scripts/build-netlify-functions.sh` → **20/20 fungsi**. `isAdminView` (fix
  logout) terkonfirmasi ada di `.netlify/functions/{ai-chat,ai-form-submit,
  admin-ai-context,get-app-data,…}.js` — semua wrapper yang meng-import
  `_lib/actions-public.ts` ikut ter-rebuild.
- `js/pages/ai_form.js` memuat guard baru (`asj_kandidat_session`).
- Rirekisho memang ikut bundel utama: `assets/app-<hash>.js` memuat
  `cetakCVRirekisho` & `buildEduRows` (`js/10_cv_rirekisho.ts` +
  `js/10b_cv_builders.ts` + `js/helpers_cv.ts`). Jadi perubahan translate AI
  TIDAK perlu file terpisah — jalurnya `ai_data_json` → `makeV(d, ai)`.
- Dua jalur deploy, keduanya benar:
  (a) Netlify build hook → `netlify.toml [build] command = bash
      scripts/build-netlify-functions.sh` → bundling jalan DI SERVER Netlify
      (publish=".", functions=".netlify/functions").
  (b) `scripts/deploy-netlify.mts` → `netlify deploy --prod --dir .`.
  `.netlify` memang di-.gitignore, tapi itu BENAR: pada jalur (a) Netlify yang
  men-generate, pada jalur (b) CLI meng-upload `--dir .` langsung dari disk
  (tidak lewat git), jadi fungsi tetap terkirim.

## ⚠️ TEMUAN NYATA: 5 kolom JP diterjemahkan AI tapi TIDAK DIRENDER di Rirekisho
Simulasi alur nyata (ai_data_json → makeV) membuktikan kolom JP yang diisi
pipeline AI **hanya sebagian** yang punya slot di template Rirekisho.

Dirender (OK, terbukti resolve):
- `tujuan_ke_jepang_jp` → baris 日本へ行く目的
- `rencana_pulang_jp` → 帰国後の目標
- `kelebihan_jp` → 長所
- `kekurangan_jp` → 短所
- `hobi_jp` → 趣味
- `riwayat_jepang` → PERNAHKEJEPANG (dibaca sebagai ID, di-map ke ADA/TIDAK)

DITERJEMAHKAN TAPI TIDAK ADA SLOT (terbuang):
- `wawancara.promosi_jp` — jumlah kolom "本人希望" / promosi diri tidak ada
- `wawancara.keahlian_jp` — 特技 tidak ada di template
- `wawancara.alasan_bidang_jp` — tidak ada
- `wawancara.keinginan_jp` — tidak ada
- `wawancara.riwayat_jepang_jp` — **khusus ini penting**: KANJI-nya (mis.
  `技能実習経験`) tidak pernah dibaca. Builder hanya baca
  `wawancara.riwayat_jepang` (nilai ID "PERNAH MAGANG") lalu mereduksinya jadi
  ADA（有）/TIDAK（無）. Jadi kolom JP deterministik yang susah payah di-prefill
  itu **hilang begitu saja** (nilainya benar & tersimpan, tapi tak tampil).

Konsekuensi: terjemahan tetap BERGUA — kolom `*_jp` tersimpan benar di
`ai_data_json` dan tampil di ai_form; yang kurang adalah **slot render** di
Rirekisho. Kalau pemilik mau kanji riwayat magang muncul di rirekisho, itu
perubahan TEMPLATE (`10b_cv_builders.ts`), bukan pipeline AI. Belum dikerjakan
— menunggu keputusan pemilik (mengubah layout dokumen resmi A4 = keputusan
pemilik, bukan keputusan teknis).

## Catatan pemetaan kunci (berguna untuk perubahan template berikutnya)
Builder rirekisho memakai `v(...keys)` dari `makeV(d, ai)` dengan prioritas
d nested → key FLAT UPPERCASE → ai. Dua keluarga kunci yang TIDAK sama:
- jp-fields/ai_form pakai nested `wawancara.kelebihan_jp`
- 10b_cv_builders membaca campuran: `KELEBIHANJP` (flat) DULU, baru
  `wawancara.kelebihan_jp`. Jadi aman.
- Tapi `tujuan_ke_jepang_jp` dibaca lewat nested + flat
  `TUJUANKEJEPANGJP/MOTIVASIKEJANGJP/MOTIVASIJP` — TIDAK ADA kunci flat
  `TUJUANKEJEPANGJP` yang di-set backend; jalur nested yang bekerja.






## Lanjutan 6 — REBUILD final (14:47, permintaan pemilik)

Permintaan: "jangan lupa di rebuild ya biar masuk ke update di service worker".

Urutan verifikasi + build yang dijalankan:
1. `npx vitest run` → **409 passed / 36 files** (naik dari 403; +6 dari
   `js/pages/ai_form-focus.test.ts`). Tidak ada regresi.
2. `npx tsc --noEmit` → bersih (exit 0).
3. `npm run build` (full) → sukses:
   - check-globals: 45 modul, 479 simbol unik, **nol kolisi**, 10 warning page
     (pre-existing, tidak terkait fix).
   - check-handlers: 193 handler unik / 331 nama terdaftar — semua terdaftar.
   - tailwind css + i18n-jp (62.9 KB, 15 domain) + build-html + build-js OK.
4. `bash scripts/build-netlify-functions.sh` → **20/20 bundled**.

Bukti fix MASUK ke artefak (bukan cuma .ts):
- `js/pages/ai_form.js` (50,150 B, 14:47) memuat guard fokus di hasil minify:
  fungsi `he()` = `setValue` → `a.value!==n&&document.activeElement!==a&&(...)`
  dan `ha()` = `updateArrayCard` → `if(i.contains(document.activeElement)){x[t]=!0;return}`.
- `assets/app-5756b589d2.js` (423,065 B, 14:47) — bundel halaman.
- `.netlify/functions/*.js` → **17 dari 20** bundel memuat `isAdminView`
  (3 sisanya tidak meng-import `actions-public`, jadi wajar).
- `sw.js` VERSION tetap `asj-portal-app-5756b589d2-mfa547b24`, SHELL memang
  sudah menunjuk `/assets/app-5756b589d2.js`. Hash **tidak berubah** karena
  hash dihitung dari konten modul sumber, bukan dari jumlah build.

Catatan penting soal "service worker update": VERSION hanya naik kalau source
berubah. Karena build ini adalah rebuild dari source yang sudah punya fix,
VERSION boleh sama — TAPI klien yang masih memegang SW lama akan mendeteksi
byte bundel berbeda dan tetap menarik versi baru. Kalau pemilik ingin
*jaminan* klien lama ikut update, cara paling andal adalah naikkan
VERSION secara eksplisit (mis. tambah suffix `-b` / timestamp) sebelum deploy.

Status: semua perbaikan (logout admin+siswa, guard fokus typing, audit
terjemahan) sudah ter-build dan siap deploy. Deploy & commit BELUM dijalankan
— menunggu izin pemilik sesuai AGENTS.md.

## Lanjutan 7 — Commit + push main, BLOKIR DEPLOY (14:55)

Permintaan: "naikkan versi biar semua user lama bisa nikmati… commit main dan
push github main supaya terdeploy ke netlify, baca env lokal".

### Yang BERHASIL
1. **Cache epoch ditambahkan** ke `scripts/build-js.mts`:
   `const CACHE_EPOCH = 2;` digabung ke VERSION →
   `asj-portal-app-5756b589d2-mfa547b24-e2`.
   Alasan: hash VERSION hanya berubah kalau konten modul berubah, jadi
   rebuild dari source yang sama menghasilkan VERSION identik dan klien
   lama tidak pernah dibujuk update. Epoch = tombol paksa manual.
   Sudah diverifikasi aman terhadap anti-loop `pwa.ts`: guard membandingkan
   STRING utuh dan key `asj_sw_purged`/`asj_sw_ver` menyimpan versi penuh,
   jadi `-e2` memicu tepat SATU purge bersih (MAX_AUTO_RELOADS=2, cooldown 30s).
2. Build ulang penuh + test: **409 test / 36 file lulus**, tsc bersih.
3. `git commit` → `3f84cb5` (23 file, +1297/-114).
4. `git push origin main` → `7f387b8..3f84cb5 main -> main` ✅

### 🔴 BLOKIR 1 — Link repo Netlify menunjuk repo yang SALAH (KOREKSI)
KOREKSI dari catatan sebelumnya: repo yang BENAR adalah
**khoci89/portallegacy** (repo aktif; last push 2026-09-12 07:56 = commit kita
3f84cb5). Remote `old-hub` (khoci921-hub/khoci921) adalah repo yang sudah
MATI sejak 2026-09-10 — bukan sebaliknya seperti yang saya tulis dulu.

Tapi `getSite` mengembalikan `repo_url: https://github.com/khoci921-hub/khoci921`
— dan kali ini field itu **BENAR**. Terbukti dari commit_ref deploy:
- deploy error 2026-09-10T02:23 → commit `3ad42ba` (hanya ada di khoci921-hub)
- deploy error 2026-09-09T09:54 → commit `20b306c` (hanya ada di khoci921-hub)
→ Netlify saat ini BENAR-BENAR menunjuk ke khoci921-hub/khoci921.
  Jadi push kita ke portallegacy TIDAK memicu build apa pun.

Perbaikan: ubah link repo situs di Netlify UI
(Site configuration → Build & deploy → Repository) dari
khoci921-hub/khoci921 → khoci89/portallegacy.

Catatan: commit `1c351b9e` (yang sedang LIVE) ada di KEDUA repo, jadi
keduanya berbagi history — khoci921-hub kemungkinan fork/copy dari
portallegacy. Perpindahan link terjadi sekitar 2026-09-09.

### 🔴 BLOKIR 2 — Kredit akun Netlify habis
Dua deploy terakhir: `state: error`,
`error_message: "Skipped due to account credit usage exceeded"`
(2026-09-09T09:54 dan 2026-09-10T02:23).
Deploy yang benar-benar published: **2026-09-09T06:02**
(`commit_ref 1c351b9e`) — artinya situs LIVE masih berisi kode 3 hari lalu
dan SEMUA perbaikan CV AI belum tampil di produksi.

### 🔴 KEAMANAN — token GitHub terekspos di URL remote
`origin` menyimpan PAT di URL:
`https://khoci89:ghp_***REDACTED***@github.com/...`
Token ini sudah tercatat di log sesi DAN sempat tertulis di file ini — GitHub
push protection MEMBLOKIR push karena itu (2026-09-12, commit ce89320).
Token sudah di-redact dari file; nilai aslinya HARUS dicabut di
https://github.com/settings/tokens lalu diganti. Remote sebaiknya pakai
kredensial manager / `gh auth`, bukan PAT inline.
JANGAN pernah menulis nilai token ke file di dalam repo ini — repo ini juga
di-push ke GitHub, dan `docs/` + root repo ikut ter-publish sebagai situs.

### Langkah berikut yang menunggu pemilik
- Perbaiki/isi kredit Netlify, ATAU
- Push ke repo yang benar (`khoci921-hub/khoci921`), ATAU
- `npx netlify deploy --prod --dir .` manual (butuh kredit tetap).

## Lanjutan 8 — ✅ DEPLOY BERHASIL (15:07) — asjportal, bukan asjportal222

Pemilik memberi token Netlify baru di `E:/Asjpow4v7-main/netlify.txt`
(berisi repo benar `khoci89/portallegacy` + token `nfp_...`).

### Temuan besar: SITUS YANG BENAR adalah `asjportal`
Token baru mengakses akun **ASJ** (`account_id 6a66bddd...be7`) dan hanya
melihat **1 situs**:
- **`asjportal`** — id `36c2ba2f-b67b-4b96-8ef0-acc75ea833b5`
  url https://asjportal.netlify.app
  repo `https://github.com/khoci89/portallegacy` ✅ BENAR
  dibuat 2026-09-11T08:35 → situs INI yang aktif dipakai.

`asjportal222` (id `129e4532-...`) yang saya selidiki sebelumnya adalah
situs LAMA/DUA yang menunjuk `khoci921-hub/khoci921`, kreditnya habis,
dan sudah ditinggalkan. Analisis saya di Lanjutan 7 soal "link repo salah"
itu benar UNTUK asjportal222, tapi asjportal222 bukan situs yang dipakai.
Catatan lama (asjportal222, khoci921-hub, "credit exceeded") = jalur buntu.

### Status deploy asjportal — SEMUA READY
Riwayat deploy (per_page 5): SEMUA `state: ready`, `error_message: null`.
- 2026-09-12T07:56:34Z → commit **3f84cb5** (fix CV AI kita)
  published 07:57:23Z ✅ ← INI YANG LIVE SEKARANG
- 2026-09-11T12:36 → 7f387b8 ✅
- 2026-09-11T12:04 → 75fcb08 ✅
- 2026-09-11T11:46 → 4907e3a ✅

Artinya auto-deploy dari GitHub → Netlify **JALAN NORMAL**. Push kita tadi
ke `origin` (portallegacy) memicu build dalam ~1 menit.

### VERIFIKASI LANGSUNG KE PRODUCTION (curl ke asjportal.netlify.app)
- `sw.js` → `const VERSION = 'asj-portal-app-5756b589d2-mfa547b24-e2'` ✅
  → cache epoch yang dinaikkan SUDAH LIVE, klien lama akan dipaksa update.
- `js/pages/ai_form.js` → ditemukan `contains(document.activeElement)`
  DAN `document.activeElement!==a` ✅ (dua guard bug typing live)
- `assets/app-5756b589d2.js` → 3 kemunculan `activeElement` ✅
- `POST /api/get-app-data` payload public → HTTP 200,
  `{"success":true,"sessionInvalid":false,...}` ✅ function hidup.

### Kesimpulan
TIDAK ADA blokir tersisa. Semua perbaikan (logout admin+siswa, bug ketik
"mental", cache epoch) SUDAH LIVE di https://asjportal.netlify.app

### Sisa pekerjaan (menunggu pemilik)
1. Token GitHub di URL remote `origin` masih terekspos di log sesi →
   cabut di https://github.com/settings/tokens, ganti `gh auth login`.
2. Token Netlify `nfp_...` tersimpan plaintext di `E:/Asjpow4v7-main/netlify.txt`
   (di LUAR repo, jadi tidak ter-commit — aman, tapi sebaiknya pindahkan
   ke kredensial manager).
3. Remote `origin` masih menyimpan PAT inline — rapikan jadi URL bersih.
4. Kolom JP rirekisho (promosi/keahlian/alasan_bidang/keinginan/
   riwayat_jepang) belum punya slot render — keputusan layout = pemilik.

---

## Fix notifikasi push FCM tidak keluar (biodata/status/reminder) — 17:29

Keluhan: notif tidak muncul di HP maupun browser saat ada kandidat update biodata.
Akar masalah ditemukan 3 (+1 risiko) dan SUDAH DIPERBAIKI di source:

1. **`fcm-server.ts` baca `process.env.FIREBASE_SERVICE_ACCOUNT` langsung**,
   bukan lewat helper `env()` (`_lib/env.ts`). Kalau kunci dipasang lewat Keys UI
   / `.env.local` (seperti secret lain), nilainya tidak pernah terbaca →
   `sendPushNotification` return false TANPA log → SEMUA push mati senyap.
   → Sekarang pakai `env('FIREBASE_SERVICE_ACCOUNT')` + `console.error` sekali
   saat kosong. **Pemilik WAJIB set `FIREBASE_SERVICE_ACCOUNT` di Netlify env.**

2. **`syncBiodataKeMail()` (`actions-mail.ts`)** punya `if (!mine.length) return;`
   SEBELUM blok `notifyAdmins` → kandidat tanpa baris lamaran update biodata
   tanpa notif apa pun. → Blok notif dipindah ke ATAS, tanpa syarat baris mail.

3. **Destructuring `.rows` dari array** (helper `supabaseJson` return array
   mentah): `actions-mail.ts` (notif status ke kandidat), `actions-schedule.ts`
   `sendToWaList`, dan `actions-schedule.ts` `database_schedule` (ini bikin
   SELURUH reminder agenda tidak pernah jalan). → Semua diperbaiki pakai
   `Array.isArray`.

4. `actions-master.ts`: `syncBiodataKeMail` dulu berada di dalam try/catch
   sinkronisasi `database_candidate` → error di situ ikut mematikan notif.
   Sekarang dipisah ke try/catch sendiri.

5. `fcm-helpers.ts`: tambah `warnOnce` — notif yang di-skip sekarang tercatat
   di log Netlify (tidak ada token admin / gagal kirim).

Verifikasi: `tsc --noEmit` bersih; `vitest run` **414 test / 38 file lulus**
(+2 file regresi baru: `actions-mail-notify.test.ts`, `fcm-server-env.test.ts`);
`scripts/build-netlify-functions.sh` sukses bundle 20 fungsi dan artifact
`.netlify/functions/*` sudah memuat kode baru.

Payload FCM sengaja tetap **data-only** (tanpa `notification`) — `sw.js`
menampilkannya lewat `onBackgroundMessage`; menambah `notification` justru
bikin notif dobel. Ini bukan bug, sudah ada test-nya.

**Sisa untuk pemilik:** isi `FIREBASE_SERVICE_ACCOUNT` di Netlify Environment
Variables, lalu pastikan admin sudah klik izinkan notifikasi (token tersimpan
di tabel `fcm_tokens` dengan `wa` = nama admin, bukan nomor 628…).

---

## Fix AKAR BUG "admin ter-logout setelah benerin CV AI kandidat" — 17:40

Keluhan ulang: sesi admin masih mati saat kembali ke panel admin setelah
mengedit CV AI siswa. Fix commit 3f84cb5 ternyata hanya menambal SATU titik
(`handleGetAppData`), akar sistemiknya belum disentuh.

### Akar masalah (ditemukan & direproduksi, bukan dugaan)
`requireRole()` (`actions-auth.ts:309`) membalas `sessionInvalid: true` untuk
**role mismatch**, bukan cuma untuk token mati. api-client menafsirkan
`sessionInvalid` sebagai "sesi habis" → **menghapus SEMUA sesi (admin +
kandidat) + reload**. Jadi satu action ber-role kandidat yang dipanggil dengan
token admin sudah cukup membunuh sesi admin yang masih sah.

Jalur nyata (kenapa terjadi "di kantor" = perangkat dipakai bersama):
panel admin → AI CV kandidat **non-VIP** di TAB YANG SAMA → `ai_form.ts`
mengalihkan ke `master-full.html?wa=…` → `master_full.ts` cek
`getCandidateSession()` yang HANYA melihat `asj_kandidat_session` (tidak cek
flag admin) → lolos kalau ada sisa sesi kandidat lama → `callAPI`
(`getMasterDataByWa` ∈ CANDIDATE_ACTIONS) mengirim **token ADMIN** karena
`asj_admin_login='sukses'` → server `requireRole(token,'kandidat')` →
sessionInvalid → semua sesi dihapus.

### Perbaikan
1. `actions-auth.ts` — `requireRole()`: token tak sah/refresh tetap
   `sessionInvalid`; **role mismatch hanya menolak aksi** (pesan
   "Akses ditolak: action ini membutuhkan sesi <role>."). Ini menutup seluruh
   kelas bug, bukan satu titik.
2. `actions-master.ts` — `handleGetMasterDataByWa()`: terima admin + kandidat
   (pola sama dengan `handleSubmitMasterForm` / `handleSimpanBerkasTahapan`).
   Import `requireRole` di file ini jadi tidak terpakai → dihapus.
3. `actions-upload.ts` — `handleSimpanRevisiKandidat()`: terima admin; kandidat
   tetap dikunci ke WA sesinya.

### Bukti
- `scripts/repro-admin-session.mts` (HTTP nyata ke preview lokal): SEBELUM →
  `getMasterDataByWa` balas `sessionInvalid=true`; SESUDAH → 0 dari 6 action.
- 4 test regresi baru di `dispatch.test.ts`; 2 di antaranya **terbukti gagal
  di kode lama** (sudah diuji dengan mengembalikan file ke HEAD lalu
  mengembalikan perbaikan). Total suite: **418 test / 38 file lulus**,
  `tsc --noEmit` bersih, artefak `.netlify/functions/*` memuat kode baru.

### Tooling baru (permanen)
- `scripts/local-preview.mts` + `npm run preview` → preview LOKAL lengkap:
  static + Netlify Functions, `.env.local` dimuat ke `process.env`, fungsi
  di-reload otomatis saat file berubah (mtime cache-bust). Port 8787
  (`PORT=` untuk ganti). Ini yang bikin bug backend bisa diuji tanpa deploy.
- `scripts/repro-admin-session.mts` + `npm run repro:session` → cek otomatis
  "apakah token admin masih dibunuh action kandidat".

### TEMUAN KEAMANAN yang BELUM diperbaiki (menunggu keputusan pemilik)
`handleGetMasterDataByWa` **tidak memeriksa kepemilikan WA**: kandidat mana pun
yang login bisa membaca biodata lengkap kandidat lain (NIK, paspor, alamat,
email) hanya dengan mengganti parameter `wa`. Komentar di `master_full.ts`
menyebut niatnya "nomor WA dari URL/QR saja tidak cukup" — jadi ini kemungkinan
celah nyata. Perbaikannya satu baris
(`if (t.role === 'kandidat' && normalizeWa(t.wa) !== normalizeWa(wa)) return …`)
tapi BELUM dipasang karena berisiko memutus alur agen/LPK yang mungkin mengisi
form untuk banyak kandidat dari satu akun. Perlu konfirmasi pemilik.

---

## AKAR MASALAH DITEMUKAN — env Firebase salah nama + fix belum pernah di-deploy (18:15)

### 1. Kenapa notifikasi tidak keluar — TERBUKTI, bukan dugaan
Dicek langsung lewat Netlify API (site `asjportal`, id `36c2ba2f-b67b-4b96-8ef0-acc75ea833b5`):

| | nama di Netlify | nama yang dibaca kode |
|---|---|---|
| service account | `FIREBASE_SERVICE_TOKEN` (2372 char, JSON valid) | `FIREBASE_SERVICE_ACCOUNT` |

19 env var terpasang; **`FIREBASE_SERVICE_ACCOUNT` tidak ada sama sekali**.
Service account-nya sendiri SAH: project `khoci-7a81c`, private_key lengkap.
→ `env('FIREBASE_SERVICE_ACCOUNT')` kosong → `sendPushNotification()` return
false → SEMUA push mati, tanpa error di log. Ini Bug #1 yang sebelumnya masih
"dugaan" — sekarang 100% terkonfirmasi.

### 2. Kenapa "masalahnya masih" — fix belum pernah masuk produksi
Deploy terakhir = commit `3f84cb5`. Commit itu MASIH memakai `requireRole` lama
(`t.role !== role` → `sessionInvalid:true`). Di produksi
`handleGetMasterDataByWa` (actions-master.ts:698) memanggil
`requireRole(token,'kandidat')`; token ADMIN → sessionInvalid → api-client
menghapus SEMUA sesi + reload → admin ter-logout.
**Semua perbaikan sesi/notif masih uncommitted di working tree.**

### 3. Temuan tambahan
- `fcm_tokens`: 52 baris, 20 token admin (wa=`KHOCI`, token asli 142 char) ✅.
  Tapi 10 baris wa=`6282130442661` = NOMOR ADMIN (ADMIN_NUMBERS 082130442661)
  salah kelas jadi token kandidat → admin itu tak pernah dapat notif admin.
- `scripts/deploy-netlify.mts` default `NETLIFY_SITE_ID` SALAH
  (`7e433a31-…` = asjportal-379); site asli `36c2ba2f-…`. `npm run deploy`
  akan menembak site yang salah.
- `.git/config` origin menyimpan GitHub PAT di URL (kredensial bocor di lokal).

### Perbaikan yang dipasang (semua sudah diverifikasi)
1. `_lib/env.ts` — `env()` resolve ALIAS saat runtime (`FIREBASE_SERVICE_TOKEN`
   → `FIREBASE_SERVICE_ACCOUNT`), + whitelist + ALIASES. **Akar Bug #1.**
2. `_lib/fcm-helpers.ts` — klasifikasi token admin: nomor yang ada di
   `ADMIN_NUMBERS` selalu dianggap admin; + dedupe token.
3. `js/pages/master_full.ts` — gerbang kandidat menerima sesi ADMIN
   (`isAdminSession()`), jadi admin tidak lagi melihat layar "login kandidat"/
   form kosong. Pengunjung tanpa sesi TETAP bergerbang (diuji).
4. `scripts/verify-fcm-credentials.mts` (baru) — bukti kredensial tanpa kirim
   notif nyata: env alias terbaca → OAuth2 Google dapat access_token → FCM
   terjangkau.
5. `e2e/_probe3.mjs` / `_probe4.mjs` (baru) — probe sesi admin + probe keamanan
   gerbang. `repro-admin-session.mts` +2 case (`submitDataAsj`, `submitMasterForm`).

### Verifikasi
- `tsc --noEmit` bersih; **437 test / 40 file lulus** (+2 file baru: env-alias,
  fcm-helpers-classify).
- `verify-fcm-credentials.mts`: `env()` → 2372 char, project khoci-7a81c,
  OAuth2 OK. Sebelum fix: 0 char, tanpa panggilan jaringan.
- Klasifikasi token: LAMA 20 admin → BARU 30 admin (+10 nomor admin), 0 regresi.
- Browser (preview lokal): 0 sessionInvalid, 0 penghapusan sesi, gerbang admin
  hilang, gerbang anonim tetap ada.
- Bundel `.netlify/functions/*` sudah memuat fix; `js/pages/master_full.js`
  sudah dibangun ulang.

### BELUM dilakukan (butuh keputusan pemilik)
Deploy. Produksi masih commit `3f84cb5`; jalur deploy = push ke `origin/main`
(Netlify build dari git, allowed branch `main`).
